services:
  sadrafars-prod:
    build:
      context: .
      dockerfile: Dockerfile
    image: sadrafars:latest
    container_name: sadrafars
    restart: unless-stopped
    networks:
      - external-net
    dns:
      - 217.218.127.127
      - 1.1.1.1

networks:
  external-net:
    external: true
